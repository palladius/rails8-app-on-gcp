[project]
name = "rails8-app-diagrams"
version = "0.1.0"
description = "Deterministic GCP Architecture Diagrams for rails8-app-on-gcp"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "diagrams>=0.23.4",
    "pillow>=10.0.0",
]

[tool.uv]
index-url = "https://pypi.org/simple"
